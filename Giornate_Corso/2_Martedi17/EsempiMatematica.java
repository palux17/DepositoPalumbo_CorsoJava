public class EsempiMatematica {
    public static void main(String[] args) {
        /*
        La classe Java Math ha molti metodi che ti consentono di eseguire compiti matematici sui numeri.
        Math.abs(x)    -> valore assoluto di dell'int fra parentesi
        Math.max(a, b) -> restituisce il maggiore tra due numeri
        Math.min(a, b) -> restituisce il minore tra due numeri
        Math.sqrt(x)   -> radice quadrata
        Math.pow(a, b) -> potenza (a elevato a b)
        Math.round(x)  -> restituisce il numero intero più vicino
        Math.ceil(x)   -> restituisce il numero intero arrotondato per eccesso
        Math.floor(x)  -> restituisce arrotonda per difetto
        Math.random()  -> restituisce numero casuale tra 0.0 e 1.0
        Math.PI        -> restituisce valore di π (3.14159...)
        Math.E         -> restituisce numero di Eulero (2.718...)
        */
    }
    
}
